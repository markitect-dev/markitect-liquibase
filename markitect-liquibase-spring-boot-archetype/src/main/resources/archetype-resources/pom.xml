#set($Integer = 0)
#set($effectiveJavaVersion = $javaVersion)
#if($springBootVersion.startsWith('3.') && $Integer.parseInt($javaVersion) < 17)
#set($effectiveJavaVersion = '17')
#elseif($javaVersion == '8')
#set($effectiveJavaVersion = '1.8')
#end
#set($mavenCompilerRelease = 'inherit')
#if(!$springBootVersion.startsWith('3.') || $springBootVersion.startsWith('3.0.'))
#if($effectiveJavaVersion == '1.8')
#set($mavenCompilerRelease = '8')
#else
#set($mavenCompilerRelease = '${java.version}')
#end
#end
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>${springBootVersion}</version>
    <relativePath/>
  </parent>

  <groupId>${groupId}</groupId>
  <artifactId>${artifactId}</artifactId>
  <version>${version}</version>

  <properties>
    <java.version>${effectiveJavaVersion}</java.version>
#if($mavenCompilerRelease != 'inherit')
    <maven.compiler.release>${mavenCompilerRelease}</maven.compiler.release>
#end

#if($h2Version != 'inherit')
    <h2.version>${h2Version}</h2.version>
#end
#if($jooqVersion != 'inherit')
    <jooq.version>${jooqVersion}</jooq.version>
#end
#if($liquibaseVersion != 'inherit')
    <liquibase.version>${liquibaseVersion}</liquibase.version>
#end
    <markitect-liquibase.version>1.0.0-SNAPSHOT</markitect-liquibase.version>
#if($snakeyamlVersion != 'inherit')
    <snakeyaml.version>${snakeyamlVersion}</snakeyaml.version>
#end
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>dev.markitect.liquibase</groupId>
        <artifactId>markitect-liquibase-bom</artifactId>
        <version>${markitect-liquibase.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter</artifactId>
        <exclusions>
          <exclusion>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <dependencies>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
    </dependency>
    <dependency>
      <groupId>dev.markitect.liquibase</groupId>
      <artifactId>markitect-liquibase-database-h2</artifactId>
    </dependency>
    <dependency>
      <groupId>dev.markitect.liquibase</groupId>
      <artifactId>markitect-liquibase-spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jooq</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-log4j2</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.liquibase</groupId>
        <artifactId>liquibase-maven-plugin</artifactId>
        <version>${liquibase.version}</version>
        <configuration>
          <driver>org.h2.Driver</driver>
          <url>jdbc:h2:${project.build.directory}/jooq-liquibase/h2</url>
          <username>sa</username>
          <password/>
          <includeArtifact>false</includeArtifact>
          <includeTestOutputDirectory>false</includeTestOutputDirectory>
          <searchPath>src/main/resources</searchPath>
          <changeLogFile>db/changelog/db.changelog-master.yaml</changeLogFile>
        </configuration>
        <executions>
          <execution>
            <id>jooq-liquibase</id>
            <goals>
              <goal>update</goal>
            </goals>
            <phase>generate-sources</phase>
          </execution>
        </executions>
        <dependencies>
          <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>${h2.version}</version>
          </dependency>
          <dependency>
            <groupId>dev.markitect.liquibase</groupId>
            <artifactId>markitect-liquibase-database-h2</artifactId>
            <version>${markitect-liquibase.version}</version>
          </dependency>
        </dependencies>
      </plugin>
      <plugin>
        <groupId>org.jooq</groupId>
        <artifactId>jooq-codegen-maven</artifactId>
        <version>${jooq.version}</version>
        <configuration>
          <jdbc>
            <driver>org.h2.Driver</driver>
            <url>jdbc:h2:${project.build.directory}/jooq-liquibase/h2</url>
            <user>sa</user>
            <password/>
          </jdbc>
          <generator>
            <database>
              <name>org.jooq.meta.h2.H2Database</name>
              <excludes>DATABASECHANGELOG.*</excludes>
              <inputSchema>PUBLIC</inputSchema>
              <outputSchemaToDefault>true</outputSchemaToDefault>
            </database>
            <target>
              <packageName>${package}.jooq</packageName>
              <directory>target/generated-sources/jooq</directory>
            </target>
          </generator>
        </configuration>
        <executions>
          <execution>
            <id>jooq-codegen</id>
            <goals>
              <goal>generate</goal>
            </goals>
            <phase>generate-sources</phase>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>

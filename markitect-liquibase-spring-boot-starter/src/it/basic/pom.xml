<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>@spring-boot.version@</version>
    <relativePath/>
  </parent>

  <groupId>invoker.it</groupId>
  <artifactId>basic-project</artifactId>
  <version>0.1-SNAPSHOT</version>

  <properties>
    <argLine/>
    <java.version>@java.version@</java.version>

    <h2.version>@h2.version@</h2.version>
    <jooq.version>@jooq.version@</jooq.version>
    <liquibase.version>@liquibase.version@</liquibase.version>
    <markitect-liquibase.version>@project.version@</markitect-liquibase.version>
    <snakeyaml.version>@snakeyaml.version@</snakeyaml.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>dev.markitect.liquibase</groupId>
        <artifactId>markitect-liquibase-bom</artifactId>
        <version>${markitect-liquibase.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>dev.markitect.liquibase</groupId>
      <artifactId>markitect-liquibase-spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jooq</artifactId>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven-surefire-plugin.version}</version>
          <configuration>
            <environmentVariables>
              <LIQUIBASE_ANALYTICS_ENABLED>false</LIQUIBASE_ANALYTICS_ENABLED>
              <LIQUIBASE_SHOW_BANNER>false</LIQUIBASE_SHOW_BANNER>
              <LIQUIBASE_SQL_LOG_LEVEL>info</LIQUIBASE_SQL_LOG_LEVEL>
            </environmentVariables>
            <argLine>@{argLine}
              -javaagent:${settings.localRepository}${file.separator}net${file.separator}bytebuddy${file.separator}byte-buddy-agent${file.separator}${byte-buddy.version}${file.separator}byte-buddy-agent-${byte-buddy.version}.jar
              -XX:-EnableDynamicAgentLoading</argLine>
            <useManifestOnlyJar>false</useManifestOnlyJar>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.liquibase</groupId>
        <artifactId>liquibase-maven-plugin</artifactId>
        <version>${liquibase.version}</version>
        <configuration>
          <driver>org.h2.Driver</driver>
          <url>jdbc:h2:${project.build.directory}${file.separator}jooq-liquibase${file.separator}h2</url>
          <username>sa</username>
          <password/>
          <includeArtifact>false</includeArtifact>
          <includeTestOutputDirectory>false</includeTestOutputDirectory>
          <searchPath>src/main/resources</searchPath>
          <changeLogFile>db/changelog/db.changelog-master.yaml</changeLogFile>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>${h2.version}</version>
          </dependency>
          <dependency>
            <groupId>dev.markitect.liquibase</groupId>
            <artifactId>markitect-liquibase-core</artifactId>
            <version>${markitect-liquibase.version}</version>
          </dependency>
        </dependencies>
        <executions>
          <execution>
            <id>jooq-liquibase</id>
            <goals>
              <goal>update</goal>
            </goals>
            <phase>generate-sources</phase>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.jooq</groupId>
        <artifactId>jooq-codegen-maven</artifactId>
        <version>${jooq.version}</version>
        <configuration>
          <jdbc>
            <driver>org.h2.Driver</driver>
            <url>jdbc:h2:${project.build.directory}${file.separator}jooq-liquibase${file.separator}h2</url>
            <user>sa</user>
            <password/>
          </jdbc>
          <generator>
            <database>
              <name>org.jooq.meta.h2.H2Database</name>
              <excludes>DATABASECHANGELOG.*</excludes>
              <inputSchema>PUBLIC</inputSchema>
              <outputSchemaToDefault>true</outputSchemaToDefault>
            </database>
            <target>
              <packageName>invoker.it.jooq</packageName>
              <directory>target/generated-sources/jooq</directory>
            </target>
          </generator>
        </configuration>
        <executions>
          <execution>
            <id>jooq-codegen</id>
            <goals>
              <goal>generate</goal>
            </goals>
            <phase>generate-sources</phase>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

</project>
